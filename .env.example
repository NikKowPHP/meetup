# -----------------------------------------------------------------------------
# Stripe Configuration (https://dashboard.stripe.com/)
# -----------------------------------------------------------------------------
# Your Stripe secret key. Found in Developers > API keys.
STRIPE_SECRET_KEY=sk_test_...

# The signing secret for your Stripe webhook. Found in Developers > Webhooks > Your Endpoint.
STRIPE_WEBHOOK_SECRET=whsec_...

# The Product ID from Stripe for your B2B event promotions. Create a product in Stripe and get its ID.
STRIPE_PROMOTION_PRODUCT_ID=prod_...


# -----------------------------------------------------------------------------
# Supabase Configuration (https://app.supabase.com/)
# -----------------------------------------------------------------------------
# Your project's database connection string for Prisma. Found in Project Settings > Database.
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[HOST]:5432/postgres"

# The secret service_role key. Found in Project Settings > API > Project API keys.
SUPABASE_SERVICE_ROLE_KEY=ey...

# The public URL for your Supabase project. Found in Project Settings > API.
NEXT_PUBLIC_SUPABASE_URL=https://...supabase.co

# The public anon key for your Supabase project. Found in Project Settings > API > Project API keys.
NEXT_PUBLIC_SUPABASE_ANON_KEY=ey...


# -----------------------------------------------------------------------------
# NextAuth Configuration (for authentication)
# -----------------------------------------------------------------------------
# A secret key for signing tokens. Generate one here: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=

# The full public URL of your deployed application.
# For local development, use: http://localhost:3000
NEXT_PUBLIC_APP_URL=https://yourapp.vercel.app

# --- NextAuth Email Provider (e.g., using Resend, SendGrid, etc.) ---
EMAIL_SERVER_HOST=smtp.resend.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=resend
EMAIL_SERVER_PASSWORD=re_...
EMAIL_FROM=onboarding@yourdomain.com

# --- NextAuth Google Provider (https://console.cloud.google.com/apis/credentials) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=


# -----------------------------------------------------------------------------
# External Service API Keys (for scraping)
# -----------------------------------------------------------------------------
# Get this from your Eventbrite developer account.
EVENTBRITE_API_KEY=

# Get this from your Meetup developer account (API Console).
MEETUP_API_KEY=


# -----------------------------------------------------------------------------
# PWA Push Notifications
# -----------------------------------------------------------------------------
# Your public VAPID key for sending web push notifications. Generate a key pair.
# You can use an online generator or a CLI tool like `npx web-push generate-vapid-keys`.
NEXT_PUBLIC_VAPID_KEY=